export const siteUrl = (
  process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"
).replace(/\/+$/, "");

/** Parent agency behind the Mining & Reputation initiative. */
export const ARBC_URL = "https://www.arbc-agency.com/";
